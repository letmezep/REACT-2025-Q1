import { Component, ReactNode } from 'react';

class SearchField extends Component {
  render(): ReactNode {
    return (
      <>
        <div className="container">SearchField Component There</div>
      </>
    );
  }
}

export default SearchField;
